[tool.black]
target-version = ["py311"]
line-length = 100

[tool.ruff]
line-length = 100
target-version = "py311"
select = ["E","F","I"]  # pycodestyle, pyflakes, isort
ignore = []

[tool.ruff.isort]
known-first-party = ["runtime"]
